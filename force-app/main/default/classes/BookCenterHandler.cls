// buuk type : CLASSICS 
// update : mode off payment == CASH ON DELIVERY
public class BookCenterHandler {
    public static void requireName(list<BOOK_CENTER__c>lstbook){
       /* for(BOOK_CENTER__c book : lstbook){
            if(book.FIRST_NAME__c == null){
                book.FIRST_NAME__c.adderror('Name is required');
            }
            
            else if(book.BOOK_TYPES__c == 'CLASSICS'){
                book.MODE_OF_PAYMENT__c = 'CASH ON DELIVERY';	
            }
            else if(book.BOOK_TYPES__c == 'FANTASY'){
                book.MODE_OF_PAYMENT__c = 'PHONE PAY';
            }
            else{
                book.MODE_OF_PAYMENT__c = 'UPI';
            }
        }  */ 
    }
}